export interface Panelist {
    name: string;
    role: string;
    institution: string;
    image: string;
}

export interface Organizer {
    name: string;
    title: string;
    role: string;
    image: string;
}
